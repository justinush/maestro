schemaVersion: "0.1"
id: example-unknown-action
version: 1.0.0
title: Unknown action type
description: Engine should fail when encountering an unknown action type

initialStepId: set
terminalStepIds: [done]

steps:
  - id: set
    kind: action
    onEnter:
      - type: http
        id: not-supported-yet

  - id: done
    kind: end

transitions:
  - from: a
    to: done
    priority: 0
